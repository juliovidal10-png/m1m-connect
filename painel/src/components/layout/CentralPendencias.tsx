"use client";

type CentralPendenciasProps = {
  onAgendaSummaryChange?: (
    summary: {
      today: number;
      overdue: number;
    },
  ) => void;
};

export default function CentralPendencias(
  _props: CentralPendenciasProps,
) {
  return null;
}
